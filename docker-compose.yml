services:
  agent_payment:
    build: .
    restart: always
    env_file:
      - .env
    ports:
      - "8150:8000"
    volumes:
      - .:/app

    networks:
    - cluster

networks:
  cluster:
    external: true
